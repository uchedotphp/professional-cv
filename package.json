{
  "name": "developers-cv",
  "version": "1.0.0",
  "description": "a developers portfolio",
  "main": "tailwind.config.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "tailwind-dev": "npx tailwindcss -i ./src/input.css -o ./src/output.css --watch",
    "sass-dev": "sass --watch src/scss:src/css",
    "combined": "npx tailwindcss -i ./src/input.css -o ./src/output.css --watch & sass --watch src/scss:src/css"
  },
  "author": "uchedotphp",
  "license": "ISC"
}
